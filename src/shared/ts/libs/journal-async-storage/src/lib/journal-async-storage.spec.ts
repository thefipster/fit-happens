import { journalAsyncStorage } from './journal-async-storage';

describe('journalAsyncStorage', () => {
  it('should work', () => {
    expect(journalAsyncStorage()).toEqual('journal-async-storage');
  });
});
